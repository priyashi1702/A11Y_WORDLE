// config.js

const gameConfig = {
    maxRounds: 5  // Number of rounds a player can play
    // playedRounds: 0,  // Tracks how many rounds have been played
    // usedWords: []  // Tracks words used as target words
};
